import type { NextPage } from "next";

const Chainprizes: NextPage = () => {
  return (
    <div className="w-screen h-screen flex align-items-center justify-center">
      <p>Tailwind + typescript bloilerplate</p>
    </div>
  );
};

export default Chainprizes;
